%h1 Listing people

%table
  %thead
    %tr
      %th First name
      %th Last name
      %th Email
      %th Phone country
      %th Phone number
      %th Verification method
      %th Verified
      %th Verification code
      %th Verification token
      %th Verified at
      %th
      %th
      %th

  %tbody
    - @people.each do |person|
      %tr
        %td= person.first_name
        %td= person.last_name
        %td= person.email
        %td= person.phone_country
        %td= person.phone_number
        %td= person.verification_method
        %td= person.verified
        %td= person.verification_code
        %td= person.verification_token
        %td= person.verified_at
        %td= link_to 'Show', person
        %td= link_to 'Edit', edit_person_path(person)
        %td= link_to 'Destroy', person, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Person', new_person_path
